server:
  port: 9092

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/

logging:
  level:
    root: info

spring:
  cloud:
    gateway:
      default-filters:
        - JwtAuthenticationFilter
