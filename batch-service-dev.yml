server:
  port: 9099

spring:
  datasource:
    url: jdbc:postgresql://localhost:5005/booker-batch-db
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9090/eureka/
